{
  "Kb": 1000.0,
  "Mb": 20.0,
  "hb": 0.1,
  "Mr": 10.0,
  "robot_limit_true": 0.5,
  "robot_limit_opt": 0.495,
  "feasibility_tolerance_m": 1e-09,
  "targets": [
    0.55,
    0.6,
    0.65,
    0.7,
    0.75
  ],
  "Kr_min": 1500.0,
  "Kr_max": 5000.0,
  "hr_min": 0.1,
  "hr_max": 0.45,
  "f0_min": 0.1,
  "f0_max": 0.45,
  "f1_min": 1.0,
  "f1_max": 4.0,
  "A_min": 0.09,
  "A_max": 0.12,
  "x_r_start_min": 0.35,
  "x_r_start_max": 0.4,
  "y_error_scale_m": 0.01,
  "x_constraint_scale_m": 0.005,
  "lambda_constraint": 10.0,
  "n_random_starts": 100,
  "adam_steps": 600,
  "learning_rate": 0.01,
  "top_k_validate": 5,
  "init_u_low": 0.02,
  "init_u_high": 0.98,
  "include_gp_de_start": false,
  "near_bound_threshold": 0.02,
  "random_seed": 2026,
  "log_every": 10,
  "run_learning_rate_sweep": true,
  "lr_sweep_target": 0.65,
  "lr_sweep_values": [
    0.01,
    0.03,
    0.05
  ],
  "lr_sweep_n_starts": 50,
  "lr_sweep_steps": 300,
  "lr_sweep_top_k_validate": 3,
  "run_nstarts_sweep": true,
  "nstarts_sweep_target": 0.65,
  "nstarts_sweep_values": [
    50,
    100
  ],
  "nstarts_sweep_steps": 300,
  "nstarts_sweep_top_k_validate": 3,
  "objective_formula": "J = ((peak_y_pred - y_target)/y_error_scale_m)^2 + lambda_constraint * max(0, (max_abs_xr_pred - robot_limit_opt)/x_constraint_scale_m)^2",
  "method_note": "NN objective has no uncertainty terms because the deterministic NN does not return predictive std. Do not compare absolute J values with GP+DE; compare final true physical metrics."
}